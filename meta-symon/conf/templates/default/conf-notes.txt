### Your shell environment is now set up for builds. ###

You can now run 'bitbake <target>'

Common target images are: core-image-minimal, core-image-weston.
Useful Commands:
  bitbake -c menuconfig virtual/kernel
  bitbake-layers show-layers
